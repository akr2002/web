const add = (a, b) => a + b;
console.log(add(2, 3));

let name = 'John';

const greet = name => `Hello ${name}`;
console.log(greet(name));

const multiply = (x, y) => {
  let result = x * y;
  return result;
};
console.log(multiply(5, 9));
